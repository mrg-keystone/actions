/** Smoke fixture for the jsr-publish reusable workflow's dry-run self-test.
 * The jsr: subpath import above exercises the preflight's export-map
 * validation against the live registry. */
export const smoke = true;
